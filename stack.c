#include "monty.h"
/**
 * f_stack - the top prints
 * @head: stack of head
 * @counter: line is number
 * Return: no for return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
